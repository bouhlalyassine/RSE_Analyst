# Minimal Streamlit Cloud dependencies.
# pandasai is intentionally not installed here: it pulls torch and pins old pandas,
# which often breaks dependency installation on Streamlit Community Cloud.
streamlit>=1.50,<2
pandas>=2.2,<3
numpy>=1.26,<3
matplotlib>=3.9,<4

st-gsheets-connection==0.1.0
streamlit-authenticator==0.2.2
streamlit-lottie==0.0.3

openpyxl>=3.1,<4
PyYAML>=6.0.2,<7
requests>=2.32,<3
